import { IsDecimalDirectiveDirective } from './is-decimal-directive.directive';

describe('IsDecimalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDecimalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
